import type { ExpenseItem } from '../types/expense';

/**
 * Default clean slate: 0 expenses.
 * Real household expenses can be added manually or selected from the Preset Catalog.
 */
export const INITIAL_EXPENSES: ExpenseItem[] = [];
